<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/PlayNoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".page.home.MainActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".page.auth.LoginActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize"
            android:launchMode="singleTask" />
        <activity
            android:name=".page.auth.RegisterActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
        <activity
            android:name=".page.article.DetailArticleActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
        <activity
            android:name=".page.collect.MyCollectActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
        <activity
            android:name=".page.points.PointsActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
        <activity
            android:name=".page.points.PointsRankActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
        <activity
            android:name=".page.search.SearchActivity"
            android:configChanges="keyboardHidden|keyboard|screenSize|locale|navigation|smallestScreenSize" />
    </application>

</manifest>